# @babybase/core

## 0.2.0

### Minor Changes

- Add `useViewTransition` option to `patchElements` for View Transitions API support. Add `source` export condition for direct TypeScript source resolution in monorepos. Add `demo` CLI command that opens a bundled demo database without writing to the user's project.
